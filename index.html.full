<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Messerjocke – AI Music Production</title>
  <meta name="description" content="Messerjocke AI Music Production – KI-generierte Musik kaufen: Jingles, Hintergrundmusik, Songs mit Gesang. Kommerzielle Lizenz, Lieferung in 48h. Pop, Latin, Electro, Lo-Fi und mehr.">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="https://bjoernhrd76-prog.github.io/messerjocke-site/">
  <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ctext y='.9em' font-size='90'%3E%F0%9F%8E%B5%3C/text%3E%3C/svg%3E">
  <link rel="sitemap" type="application/xml" href="/sitemap.xml">

  <!-- Open Graph -->
  <meta property="og:title" content="Messerjocke – AI Music Production">
  <meta property="og:description" content="Custom AI-Musik: Jingles, Background Tracks, Full Songs. Lieferung in unter 48h.">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://bjoernhrd76-prog.github.io/messerjocke-site/">

  <!-- Preconnect -->
  <link rel="preconnect" href="https://open.spotify.com" crossorigin>
  <link rel="dns-prefetch" href="https://open.spotify.com">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Messerjocke – AI Music Production">
  <meta name="twitter:description" content="KI-Musik für Creator & Brands. Jetzt anfragen.">

  <style>
    :root {
      --bg: #0a0a0f;
      --surface: #12121a;
      --surface2: #1a1a26;
      --border: #1e1e2e;
      --text: #e8e8f0;
      --muted: #8888a0;
      --accent: #7c5cff;
      --accent-glow: rgba(124, 92, 255, 0.25);
      --hot: #ff4d6a;
      --green: #22c55e;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 2rem;
      background: rgba(10,10,15,0.85);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-bottom: 1px solid rgba(30,30,46,0.6);
      transition: background 0.3s;
    }

    .nav-logo {
      font-size: 1.1rem;
      font-weight: 800;
      letter-spacing: -0.02em;
      color: var(--text);
      text-decoration: none;
    }

    .nav-logo span {
      color: var(--accent);
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      color: var(--muted);
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: 500;
      transition: color 0.2s;
    }

    .nav-links a:hover { color: var(--text); }
    .nav-links a.active { color: var(--accent); }

    .nav-cta {
      display: inline-flex;
      align-items: center;
      padding: 0.55rem 1.25rem;
      background: var(--accent);
      color: #fff;
      text-decoration: none;
      border-radius: 8px;
      font-size: 0.9rem;
      font-weight: 600;
      transition: all 0.2s;
    }

    .nav-cta:hover {
      transform: translateY(-1px);
      box-shadow: 0 4px 16px var(--accent-glow);
    }

    .nav-toggle {
      display: none;
      flex-direction: column;
      gap: 5px;
      background: none;
      border: none;
      cursor: pointer;
      padding: 4px;
      z-index: 101;
    }

    .nav-toggle span {
      display: block;
      width: 22px;
      height: 2px;
      background: var(--text);
      border-radius: 2px;
      transition: all 0.3s;
    }

    .nav-toggle.active span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
    .nav-toggle.active span:nth-child(2) { opacity: 0; }
    .nav-toggle.active span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }

    @media (max-width: 640px) {
      .nav-toggle { display: flex; }
      .nav-links {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        flex-direction: column;
        background: rgba(10,10,15,0.97);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        padding: 1rem 2rem 1.5rem;
        gap: 0;
        border-bottom: 1px solid var(--border);
      }
      .nav-links.open { display: flex; }
      .nav-links li { padding: 0.75rem 0; border-bottom: 1px solid rgba(255,255,255,0.04); }
      .nav-links li:last-child { border-bottom: none; }
      .nav-links a { font-size: 1rem; }
    }

    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 7rem 2rem 4rem;
      background:
        radial-gradient(ellipse at 30% 20%, rgba(124,92,255,0.07) 0%, transparent 50%),
        radial-gradient(ellipse at 70% 80%, rgba(255,77,106,0.05) 0%, transparent 50%);
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%237c5cff' fill-opacity='0.02'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.4rem 1rem;
      border: 1px solid var(--border);
      border-radius: 100px;
      font-size: 0.8rem;
      color: var(--accent);
      letter-spacing: 0.08em;
      text-transform: uppercase;
      margin-bottom: 2rem;
      background: rgba(124,92,255,0.05);
      position: relative;
    }

    .badge-dot {
      width: 7px;
      height: 7px;
      background: var(--green);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(0.8); }
    }

    .hero h1 {
      font-size: clamp(2.5rem, 6vw, 4.5rem);
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -0.03em;
      margin-bottom: 1.5rem;
      position: relative;
    }

    .hero h1 .grad {
      background: linear-gradient(135deg, var(--accent) 0%, var(--hot) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-sub {
      font-size: 1.15rem;
      color: var(--muted);
      max-width: 520px;
      margin-bottom: 2.5rem;
      position: relative;
    }

    .cta-row {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
      position: relative;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.9rem 2rem;
      border-radius: 10px;
      font-size: 1rem;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.2s;
      cursor: pointer;
      border: none;
    }

    .btn-primary {
      background: var(--accent);
      color: #fff;
      box-shadow: 0 4px 24px var(--accent-glow);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 32px rgba(124,92,255,0.4);
    }

    .btn-outline {
      background: transparent;
      color: var(--text);
      border: 1px solid var(--border);
    }

    .btn-outline:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    .btn-spotify {
      background: #1DB954;
      color: #fff;
    }

    .btn-spotify:hover {
      background: #1ed760;
      transform: translateY(-2px);
    }

    .hero-note {
      margin-top: 1.5rem;
      font-size: 0.82rem;
      color: var(--muted);
      position: relative;
    }

    .stats {
      display: flex;
      gap: 0;
      justify-content: center;
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      flex-wrap: wrap;
    }

    .stat {
      text-align: center;
      padding: 2.5rem 3rem;
      border-right: 1px solid var(--border);
      flex: 1;
      min-width: 140px;
    }

    .stat:last-child { border-right: none; }

    .stat-num {
      font-size: 2.2rem;
      font-weight: 800;
      color: var(--accent);
      letter-spacing: -0.02em;
    }

    .stat-label {
      font-size: 0.82rem;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.06em;
      margin-top: 0.25rem;
    }

    .section {
      max-width: 1100px;
      margin: 0 auto;
      padding: 5rem 2rem;
    }

    .section-label {
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--accent);
      font-weight: 600;
      margin-bottom: 0.75rem;
    }

    .section-title {
      font-size: 2rem;
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 0.5rem;
    }

    .section-sub {
      color: var(--muted);
      margin-bottom: 3rem;
      max-width: 580px;
      font-size: 1rem;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.25rem;
    }

    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.75rem;
      transition: all 0.25s;
      position: relative;
      overflow: hidden;
    }

    .card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--accent), var(--hot));
      transform: scaleX(0);
      transition: transform 0.3s;
    }

    .card:hover { border-color: var(--accent); transform: translateY(-2px); }
    .card:hover::before { transform: scaleX(1); }

    .card-icon {
      font-size: 1.75rem;
      margin-bottom: 1rem;
      display: block;
    }

    .card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .card p {
      color: var(--muted);
      font-size: 0.92rem;
      line-height: 1.6;
    }

    .listen-section {
      background: var(--surface);
      border-top: 1px solid var(--border);
      border-bottom: 1px solid var(--border);
      padding: 5rem 2rem;
      text-align: center;
    }

    .listen-section .section-title { margin-bottom: 0.5rem; }

    .spotify-embed-wrap {
      max-width: 700px;
      margin: 2.5rem auto 0;
      border-radius: 14px;
      overflow: hidden;
      box-shadow: 0 8px 40px rgba(0,0,0,0.5);
    }

    .spotify-embed-wrap iframe {
      display: block;
      width: 100%;
      border: none;
    }

    .listen-cta {
      margin-top: 2rem;
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .platforms {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
      margin-top: 2rem;
    }

    .platform-chip {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1.1rem;
      background: var(--surface2);
      border: 1px solid var(--border);
      border-radius: 100px;
      font-size: 0.85rem;
      font-weight: 500;
      color: var(--muted);
    }

    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 1.25rem;
    }

    .price-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 2rem;
      display: flex;
      flex-direction: column;
    }

    .price-card.featured {
      border-color: var(--accent);
      box-shadow: 0 0 40px var(--accent-glow);
      position: relative;
    }

    .featured-badge {
      position: absolute;
      top: -1px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--accent);
      color: #fff;
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      padding: 0.3rem 1rem;
      border-radius: 0 0 8px 8px;
    }

    .price-card h3 {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--muted);
      margin-bottom: 0.25rem;
    }

    .price-tag {
      font-size: 2.8rem;
      font-weight: 800;
      letter-spacing: -0.03em;
      margin: 0.5rem 0;
      line-height: 1;
    }

    .price-tag small {
      font-size: 0.9rem;
      font-weight: 400;
      color: var(--muted);
    }

    .price-desc {
      font-size: 0.88rem;
      color: var(--muted);
      margin-bottom: 1.5rem;
    }

    .price-card ul {
      list-style: none;
      margin: 0 0 2rem;
      flex: 1;
    }

    .price-card li {
      padding: 0.45rem 0;
      color: var(--muted);
      font-size: 0.9rem;
      display: flex;
      align-items: center;
      gap: 0.6rem;
      border-bottom: 1px solid rgba(255,255,255,0.04);
    }

    .price-card li::before {
      content: '';
      width: 16px;
      height: 16px;
      border-radius: 50%;
      background: rgba(124,92,255,0.15);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      background-image: url("data:image/svg+xml,%3Csvg width='10' height='8' viewBox='0 0 10 8' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 4L3.5 6.5L9 1' stroke='%237c5cff' stroke-width='1.5' stroke-linecap='round' stroke-linejoin='round'/%3E%3C/svg%3E");
      background-repeat: no-repeat;
      background-position: center;
    }

    .faq-list {
      display: flex;
      flex-direction: column;
      gap: 0;
      border: 1px solid var(--border);
      border-radius: 14px;
      overflow: hidden;
    }

    .faq-item {
      border-bottom: 1px solid var(--border);
    }

    .faq-item:last-child { border-bottom: none; }

    .faq-q {
      width: 100%;
      text-align: left;
      background: var(--surface);
      border: none;
      padding: 1.25rem 1.5rem;
      font-size: 0.98rem;
      font-weight: 600;
      color: var(--text);
      cursor: pointer;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: background 0.2s;
    }

    .faq-q:hover { background: var(--surface2); }

    .faq-q .chevron {
      font-size: 1rem;
      color: var(--muted);
      transition: transform 0.25s;
      flex-shrink: 0;
    }

    .faq-item.open .faq-q { background: var(--surface2); }
    .faq-item.open .chevron { transform: rotate(180deg); }

    .faq-a {
      display: none;
      padding: 0 1.5rem 1.25rem;
      background: var(--surface2);
      color: var(--muted);
      font-size: 0.92rem;
      line-height: 1.7;
    }

    .faq-item.open .faq-a { display: block; }

    .contact-section {
      background: radial-gradient(ellipse at 50% 0%, rgba(124,92,255,0.08) 0%, transparent 60%);
      border-top: 1px solid var(--border);
      text-align: center;
      padding: 6rem 2rem;
    }

    .contact-email {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--accent);
      text-decoration: none;
      font-size: 1.3rem;
      font-weight: 700;
      margin: 1.5rem 0;
      transition: opacity 0.2s;
    }

    .contact-email:hover { opacity: 0.8; }

    .contact-meta {
      color: var(--muted);
      font-size: 0.88rem;
      margin-top: 0.5rem;
    }

    .contact-alts {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
      margin-top: 2rem;
    }

    footer {
      text-align: center;
      padding: 2rem;
      color: var(--muted);
      font-size: 0.82rem;
      border-top: 1px solid var(--border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 1rem;
      max-width: 1100px;
      margin: 0 auto;
    }

    footer a {
      color: var(--muted);
      text-decoration: none;
    }

    footer a:hover { color: var(--text); }

    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .fade-in.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .fade-in-delay-1 { transition-delay: 0.1s; }
    .fade-in-delay-2 { transition-delay: 0.2s; }
    .fade-in-delay-3 { transition-delay: 0.3s; }

    @media (max-width: 640px) {
      .stat { padding: 1.75rem 1.25rem; border-right: none; border-bottom: 1px solid var(--border); }
      .stat:last-child { border-bottom: none; }
      .cta-row { flex-direction: column; align-items: center; }
      .contact-alts { flex-direction: column; align-items: center; }
      footer { justify-content: center; }
    }
  </style>

  <!-- Schema.org Structured Data -->
  <script type="application/ld+json">
  [{
    "@context": "https://schema.org",
    "@type": "MusicGroup",
    "name": "Messerjocke",
    "url": "https://bjoernhrd76-prog.github.io/messerjocke-site/",
    "genre": ["Pop", "Latin", "Electro", "French Pop", "Lo-Fi", "Ambient", "Cinematic"],
    "description": "KI-generierte Musik fuer Creator, Marken und Unternehmen. Jingles, Hintergrundmusik und vollstaendige Songs.",
    "sameAs": [
      "https://open.spotify.com/artist/4DhynD1iJwYZqwdwCwu2PD",
      "https://music.apple.com/search?term=Messerjocke"
    ],
    "email": "messerjocke@proton.me"
  },
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": "Messerjocke AI Music Production",
    "url": "https://bjoernhrd76-prog.github.io/messerjocke-site/",
    "email": "messerjocke@proton.me",
    "description": "AI Music Production Service – Jingles, Hintergrundmusik, Songs mit kommerzieller Lizenz. Lieferung in unter 48 Stunden.",
    "priceRange": "€79–€249",
    "knowsLanguage": ["de", "en", "es", "fr"],
    "makesOffer": [
      {
        "@type": "Offer",
        "name": "Starter",
        "price": "79",
        "priceCurrency": "EUR",
        "description": "2 fertige Songs im Wunschgenre mit kommerzieller Lizenz"
      },
      {
        "@type": "Offer",
        "name": "Content Creator Pack",
        "price": "249",
        "priceCurrency": "EUR",
        "description": "10 Songs in 5 Stimmungen fuer Content-Produktion mit kommerzieller Lizenz"
      },
      {
        "@type": "Offer",
        "name": "Custom Production",
        "price": "249",
        "priceCurrency": "EUR",
        "description": "2 massgeschneiderte Songs nach persoenlichem Briefing mit Cover Art"
      }
    ]
  }]
  </script>
</head>
<body>

  <!-- NAV -->
  <nav aria-label="Hauptnavigation">
    <a href="#" class="nav-logo" aria-label="Messerjocke Startseite">Messer<span>jocke</span></a>
    <button class="nav-toggle" aria-label="Menue oeffnen" aria-expanded="false" onclick="toggleMobileMenu()">
      <span></span><span></span><span></span>
    </button>
    <ul class="nav-links" id="nav-links" role="navigation">
      <li><a href="services.html" aria-label="Alle Services ansehen">Services</a></li>
      <li><a href="about.html" aria-label="Ueber Messerjocke">About</a></li>
      <li><a href="email-service.html" aria-label="Email-Automatisierung">Email-Automation</a></li>
      <li><a href="kontakt.html" aria-label="Kontaktseite">Kontakt</a></li>
      <li><a href="blog/" aria-label="Blog">Blog</a></li>
    </ul>
    <a href="mailto:messerjocke@proton.me?subject=Music%20Anfrage" class="nav-cta" aria-label="Anfrage per E-Mail senden">Anfrage senden</a>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-badge">
      <span class="badge-dot"></span>
      KI-Musik · Live auf Spotify &amp; Apple Music
    </div>
    <h1>Musik, die <span class="grad">bewegt</span>.<br>Produziert von KI.<br>Kuratiert von Menschen.</h1>
    <p class="hero-sub">KI-generierte Musik für Creator, Marken und Unternehmen. Jingles, Hintergrundtracks, vollständige Songs — schnell geliefert, fair berechnet.</p>
    <div class="cta-row">
      <a href="mailto:messerjocke@proton.me?subject=Music%20Anfrage" class="btn btn-primary" aria-label="Jetzt per E-Mail anfragen">
        ✉ Jetzt anfragen
      </a>
      <a href="#listen" class="btn btn-spotify" aria-label="Musik-Beispiele anhoeren">
        ▶ Musik hören
      </a>
      <a href="#services" class="btn btn-outline" aria-label="Unsere Services ansehen">Services ansehen</a>
    </div>
    <p class="hero-note">Erste Antwort innerhalb von 24 Stunden &nbsp;·&nbsp; Lieferung in unter 48h</p>
  </section>

  <!-- STATS -->
  <div class="stats">
    <div class="stat fade-in">
      <div class="stat-num">60+</div>
      <div class="stat-label">Produzierte Tracks</div>
    </div>
    <div class="stat fade-in fade-in-delay-1">
      <div class="stat-num">37+</div>
      <div class="stat-label">Songs auf Streaming</div>
    </div>
    <div class="stat fade-in fade-in-delay-2">
      <div class="stat-num">5+</div>
      <div class="stat-label">Genres &amp; Sprachen</div>
    </div>
    <div class="stat fade-in fade-in-delay-3">
      <div class="stat-num">&lt;48h</div>
      <div class="stat-label">Lieferzeit</div>
    </div>
  </div>

  <!-- SERVICES -->
  <section class="section" id="services">
    <p class="section-label">Was wir machen</p>
    <h2 class="section-title">Musik für jeden Bedarf</h2>
    <p class="section-sub">KI-generiert, menschlich kuratiert. Jeder Track wird bewertet, geprüft und gemastert, bevor er ausgeliefert wird.</p>
    <div class="grid">
      <div class="card fade-in">
        <span class="card-icon">🎵</span>
        <h3>Jingles &amp; Intros</h3>
        <p>Kurze, prägnante Tracks für Podcasts, YouTube-Kanäle, Markenvideos und Apps. 5–30 Sekunden, jeder Stil.</p>
      </div>
      <div class="card fade-in fade-in-delay-1">
        <span class="card-icon">🎧</span>
        <h3>Hintergrundmusik</h3>
        <p>Ambient, Lo-Fi, Cinematic oder Upbeat. Perfekt für Content, Werbung, Präsentationen und Wartebereiche.</p>
      </div>
      <div class="card fade-in fade-in-delay-2">
        <span class="card-icon">🎤</span>
        <h3>Vollständige Songs</h3>
        <p>Komplette Songs mit Gesang, Lyrics und Produktion. Pop, Latin, Electro, French Pop und mehr.</p>
      </div>
      <div class="card fade-in">
        <span class="card-icon">⚡</span>
        <h3>Custom Production</h3>
        <p>Beschreibe was du brauchst. Genre, Stimmung, Tempo, Sprache. Wir produzieren, du genehmigst. Revisionen inklusive.</p>
      </div>
      <div class="card fade-in fade-in-delay-1">
        <span class="card-icon">🎬</span>
        <h3>Social Media Assets</h3>
        <p>Fertig für TikTok, Reels und YouTube Shorts. Musik + Visuals + Captions in einem Paket.</p>
      </div>
      <div class="card fade-in fade-in-delay-2">
        <span class="card-icon">📦</span>
        <h3>Musik-Packs</h3>
        <p>Vorgefertigte Bundles: 5 oder 10 Tracks zu einem Thema. Herunterladen, nutzen, fertig. Kommerzielle Lizenz inklusive.</p>
      </div>
    </div>
  </section>

  <!-- FEATURED TRACK PLAYER -->
  <section class="section" id="hoerprobe" style="text-align:center;padding:4rem 2rem;">
    <p class="section-label">H&ouml;rprobe</p>
    <h2 class="section-title">Reinhören. Überzeugen.</h2>
    <p style="color:var(--muted);margin:-0.5rem auto 2.5rem;max-width:480px;font-size:0.95rem;">So klingt KI-produzierte Musik von Messerjocke. Professionell, emotional, sofort einsetzbar.</p>

    <div class="featured-player" id="featuredPlayer">
      <div class="fp-cover-wrap">
        <img src="audio/cover-wir-reissen-alles-nieder-web.jpg" alt="Wir reissen alles nieder — Cover" class="fp-cover" id="fpCover">
        <div class="fp-glow"></div>
        <button class="fp-play-btn" id="fpPlayBtn" aria-label="Abspielen">
          <svg class="fp-icon-play" viewBox="0 0 24 24" fill="currentColor" width="32" height="32"><path d="M8 5v14l11-7z"/></svg>
          <svg class="fp-icon-pause" viewBox="0 0 24 24" fill="currentColor" width="32" height="32" style="display:none"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>
        </button>
      </div>
      <div class="fp-info">
        <div class="fp-title">Wir reissen alles nieder</div>
        <div class="fp-artist">Messerjocke &middot; Punk Rock</div>
        <div class="fp-progress-wrap" id="fpProgressWrap">
          <span class="fp-time" id="fpTimeCurrent">0:00</span>
          <div class="fp-bar" id="fpBar">
            <div class="fp-bar-fill" id="fpBarFill"></div>
            <div class="fp-bar-glow" id="fpBarGlow"></div>
          </div>
          <span class="fp-time" id="fpTimeDuration">0:00</span>
        </div>
      </div>
    </div>
  </section>

  <style>
    .featured-player {
      max-width: 520px; margin: 0 auto;
      background: var(--surface); border: 1px solid var(--border);
      border-radius: 18px; padding: 1.5rem;
      display: flex; align-items: center; gap: 1.5rem;
      position: relative; overflow: hidden;
      transition: border-color 0.3s, box-shadow 0.3s;
    }
    .featured-player.is-playing {
      border-color: var(--accent);
      box-shadow: 0 0 40px rgba(124,92,255,0.15), 0 0 80px rgba(124,92,255,0.05);
    }
    .fp-cover-wrap {
      position: relative; flex-shrink: 0;
      width: 110px; height: 110px;
    }
    .fp-cover {
      width: 110px; height: 110px; border-radius: 12px;
      object-fit: cover; display: block;
      box-shadow: 0 4px 20px rgba(0,0,0,0.4);
    }
    .fp-glow {
      position: absolute; inset: -8px; border-radius: 16px;
      background: radial-gradient(circle, rgba(124,92,255,0.25) 0%, transparent 70%);
      opacity: 0; transition: opacity 0.4s; pointer-events: none; z-index: 0;
    }
    .featured-player.is-playing .fp-glow { opacity: 1; }
    .fp-play-btn {
      position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%);
      width: 48px; height: 48px; border-radius: 50%; border: none;
      background: rgba(124,92,255,0.9); color: #fff;
      display: flex; align-items: center; justify-content: center;
      cursor: pointer; z-index: 2; backdrop-filter: blur(4px);
      transition: background 0.2s, transform 0.15s;
      box-shadow: 0 2px 12px rgba(124,92,255,0.4);
    }
    .fp-play-btn:hover { background: #9b7dff; transform: translate(-50%, -50%) scale(1.08); }
    .fp-play-btn:active { transform: translate(-50%, -50%) scale(0.95); }
    .fp-info { flex: 1; min-width: 0; text-align: left; }
    .fp-title {
      font-size: 1.15rem; font-weight: 700; color: var(--text);
      white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
      letter-spacing: -0.01em;
    }
    .fp-artist {
      font-size: 0.82rem; color: var(--accent); font-weight: 600;
      text-transform: uppercase; letter-spacing: 0.06em; margin-top: 0.2rem;
    }
    .fp-progress-wrap {
      display: flex; align-items: center; gap: 0.6rem; margin-top: 1rem;
    }
    .fp-time {
      font-size: 0.75rem; color: var(--muted);
      font-variant-numeric: tabular-nums; min-width: 32px;
    }
    .fp-bar {
      flex: 1; height: 5px; background: var(--surface2);
      border-radius: 3px; cursor: pointer; position: relative; overflow: hidden;
    }
    .fp-bar-fill {
      height: 100%; width: 0%; border-radius: 3px;
      background: linear-gradient(90deg, var(--accent), #a78bfa);
      transition: width 0.15s linear; pointer-events: none;
    }
    .fp-bar-glow {
      position: absolute; top: -3px; height: 11px; width: 20px;
      border-radius: 50%; left: 0%;
      background: rgba(124,92,255,0.5); filter: blur(6px);
      pointer-events: none; opacity: 0; transition: opacity 0.3s;
    }
    .featured-player.is-playing .fp-bar-glow { opacity: 1; }

    @media (max-width: 480px) {
      .featured-player { flex-direction: column; text-align: center; padding: 1.25rem; }
      .fp-info { text-align: center; }
      .fp-cover-wrap { width: 140px; height: 140px; }
      .fp-cover { width: 140px; height: 140px; }
    }
  </style>

  <script>
  (function() {
    var audio = new Audio();
    audio.preload = 'none';
    var player = document.getElementById('featuredPlayer');
    var playBtn = document.getElementById('fpPlayBtn');
    var iconPlay = playBtn.querySelector('.fp-icon-play');
    var iconPause = playBtn.querySelector('.fp-icon-pause');
    var barFill = document.getElementById('fpBarFill');
    var barGlow = document.getElementById('fpBarGlow');
    var bar = document.getElementById('fpBar');
    var timeCur = document.getElementById('fpTimeCurrent');
    var timeDur = document.getElementById('fpTimeDuration');

    function fmt(s) {
      if (isNaN(s)) return '0:00';
      var m = Math.floor(s / 60);
      var sec = Math.floor(s % 60);
      return m + ':' + (sec < 10 ? '0' : '') + sec;
    }

    playBtn.addEventListener('click', function() {
      if (!audio.src || audio.src === '') {
        audio.src = 'audio/wir-reissen-alles-nieder.mp3';
      }
      if (audio.paused) {
        audio.play().catch(function(){});
        player.classList.add('is-playing');
        iconPlay.style.display = 'none';
        iconPause.style.display = '';
      } else {
        audio.pause();
        player.classList.remove('is-playing');
        iconPlay.style.display = '';
        iconPause.style.display = 'none';
      }
    });

    audio.addEventListener('loadedmetadata', function() {
      timeDur.textContent = fmt(audio.duration);
    });

    audio.addEventListener('timeupdate', function() {
      if (!audio.duration) return;
      var pct = (audio.currentTime / audio.duration) * 100;
      barFill.style.width = pct + '%';
      barGlow.style.left = 'calc(' + pct + '% - 10px)';
      timeCur.textContent = fmt(audio.currentTime);
    });

    bar.addEventListener('click', function(e) {
      if (!audio.duration) return;
      var rect = bar.getBoundingClientRect();
      var pct = (e.clientX - rect.left) / rect.width;
      audio.currentTime = pct * audio.duration;
    });

    audio.addEventListener('ended', function() {
      player.classList.remove('is-playing');
      iconPlay.style.display = '';
      iconPause.style.display = 'none';
      barFill.style.width = '0%';
      timeCur.textContent = '0:00';
    });
  })();
  </script>

  <!-- LISTEN -->
  <section class="listen-section" id="listen">
    <p class="section-label" style="text-align:center;">Jetzt anhören</p>
    <h2 class="section-title">Unsere Musik auf Streaming</h2>
    <p style="color:var(--muted);margin-top:0.5rem;font-size:0.95rem;">Weltweit verfügbar über DistroKid — auf allen großen Plattformen.</p>

    <!-- Spotify Embed (Artist Page) -->
    <div class="spotify-embed-wrap fade-in">
      <iframe
        src="https://open.spotify.com/embed/artist/4DhynD1iJwYZqwdwCwu2PD?utm_source=generator&theme=0"
        width="100%"
        height="352"
        frameBorder="0"
        allowfullscreen=""
        allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
        loading="lazy">
      </iframe>
    </div>

    <div class="listen-cta">
      <a href="https://open.spotify.com/artist/4DhynD1iJwYZqwdwCwu2PD" target="_blank" rel="noopener" class="btn btn-spotify" aria-label="Messerjocke auf Spotify oeffnen">
        ▶ Auf Spotify öffnen
      </a>
      <a href="https://music.apple.com/search?term=Messerjocke" target="_blank" rel="noopener" class="btn btn-outline" aria-label="Messerjocke auf Apple Music suchen">
        🎵 Apple Music
      </a>
    </div>

    <div class="platforms">
      <span class="platform-chip">✓ Spotify</span>
      <span class="platform-chip">✓ Apple Music</span>
      <span class="platform-chip">✓ Amazon Music</span>
      <span class="platform-chip">✓ YouTube Music</span>
      <span class="platform-chip">✓ Deezer</span>
      <span class="platform-chip">✓ Tidal</span>
      <span class="platform-chip">+ 30 weitere</span>
    </div>
  </section>

  <!-- PRICING -->
  <section class="section" id="pricing">
    <p class="section-label">Preise</p>
    <h2 class="section-title">Klare Preise. Kein Abo.</h2>
    <p class="section-sub">Einmal zahlen, sofort nutzen. Jeder Track mit kommerzieller Lizenz.</p>
    <div class="pricing-grid">

      <div class="price-card fade-in">
        <h3>Starter</h3>
        <div class="price-tag">€79</div>
        <p class="price-desc">Dein erster eigener KI-Song. Professionell produziert, sofort einsetzbar.</p>
        <ul>
          <li>2 fertige Songs im Wunschgenre</li>
          <li>Du w&auml;hlst deinen Favoriten &mdash; oder beh&auml;ltst beide</li>
          <li>MP3 + WAV in Studioqualit&auml;t</li>
          <li>Kommerzielle Nutzungsrechte inklusive</li>
          <li>Lieferung in 24&ndash;48 Stunden</li>
        </ul>
        <a href="mailto:messerjocke@proton.me?subject=Starter%20Anfrage" class="btn btn-outline" style="width:100%;justify-content:center;">Jetzt bestellen</a>
      </div>

      <div class="price-card featured fade-in fade-in-delay-1">
        <div class="featured-badge">Beliebteste Wahl</div>
        <h3>Content Creator Pack</h3>
        <div class="price-tag">€249</div>
        <p class="price-desc">Deine Sound-Bibliothek f&uuml;r YouTube, Podcast und Social Media.</p>
        <ul>
          <li>10 Songs aus 5 Stimmungen &mdash; du w&auml;hlst die besten</li>
          <li>Perfekt f&uuml;r Intros, Hintergrund und Content</li>
          <li>MP3 + WAV in Studioqualit&auml;t</li>
          <li>Kommerzielle Nutzungsrechte inklusive</li>
          <li>1 Revision pro Stimmung</li>
        </ul>
        <a href="mailto:messerjocke@proton.me?subject=Creator%20Pack%20Anfrage" class="btn btn-primary" style="width:100%;justify-content:center;">Jetzt bestellen</a>
      </div>

      <div class="price-card fade-in fade-in-delay-2">
        <h3>Custom Production</h3>
        <div class="price-tag">€249</div>
        <p class="price-desc">Dein Song. Dein Briefing. Ma&szlig;geschneidert auf deine Marke.</p>
        <ul>
          <li>2 ma&szlig;geschneiderte Songs nach pers&ouml;nlichem Briefing</li>
          <li>Genre, Sprache, Stimmung und Schl&uuml;sselw&ouml;rter nach Wunsch</li>
          <li>MP3 + WAV in Studioqualit&auml;t</li>
          <li>Cover Art inklusive</li>
          <li>Bis zu 3 Revisionen</li>
        </ul>
        <a href="mailto:messerjocke@proton.me?subject=Custom%20Production%20Anfrage" class="btn btn-outline" style="width:100%;justify-content:center;">Anfrage starten</a>
      </div>

    </div>
  </section>

  <!-- FAQ -->
  <section class="section" id="faq">
    <p class="section-label">Häufige Fragen</p>
    <h2 class="section-title">FAQ</h2>
    <p class="section-sub">Du hast Fragen? Wir haben Antworten.</p>

    <div class="faq-list">
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          Wie lange dauert die Lieferung?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Standard-Lieferzeit sind 24–48 Stunden nach Auftragseingang. Für komplexe Custom Tracks kann es bis zu 72 Stunden dauern. Auf Wunsch ist Express-Lieferung möglich.
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          Darf ich die Musik kommerziell nutzen?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Ja. Alle Tracks kommen mit vollständiger kommerzieller Lizenz. Du darfst sie in YouTube-Videos, Werbung, Podcasts, Apps und anderen kommerziellen Projekten nutzen.
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          Welche Genres sind möglich?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Wir produzieren Pop, Latin, Electro, French Pop, Deutschpop, Lo-Fi, Ambient, Cinematic und mehr. Wenn du ein spezifisches Genre oder eine Stimmung im Kopf hast, schreib uns einfach.
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          Was passiert, wenn mir der Track nicht gefällt?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Creator Pack enth&auml;lt 1 Revision pro Stimmung, Custom Production enth&auml;lt bis zu 3 Revisionen. Wir arbeiten so lange, bis du zufrieden bist. Dein Brief ist unser Ausgangspunkt — je detaillierter, desto besser das Ergebnis.
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          Bekomme ich die Stems (einzelne Spuren)?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Stems (einzelne Spuren f&uuml;r Gesang, Instrumente, Rhythmus) sind auf Anfrage verf&uuml;gbar. Sprich uns einfach an &mdash; wir kl&auml;ren, was f&uuml;r dein Projekt m&ouml;glich ist.
        </div>
      </div>
      <div class="faq-item">
        <button class="faq-q" onclick="toggleFaq(this)">
          In welchen Sprachen produziert ihr?
          <span class="chevron">▾</span>
        </button>
        <div class="faq-a">
          Aktuell: Deutsch, Englisch, Spanisch und Französisch. Weitere Sprachen auf Anfrage.
        </div>
      </div>
    </div>
  </section>

  <!-- CONTACT -->
  <section class="contact-section" id="contact">
    <p class="section-label" style="text-align:center;">Kontakt</p>
    <h2 class="section-title">Bereit loszulegen?</h2>
    <p style="color:var(--muted);margin-top:0.5rem;font-size:0.95rem;">Schick uns dein Brief. Erste Antwort innerhalb von 24 Stunden.</p>
    <div style="margin-top:2rem;">
      <a href="mailto:messerjocke@proton.me?subject=Music%20Anfrage" class="contact-email">
        ✉ messerjocke@proton.me
      </a>
      <p class="contact-meta">Oder nutze einen der direkten Links:</p>
    </div>
    <div class="contact-alts">
      <a href="mailto:messerjocke@proton.me?subject=Starter%20Pack%20Anfrage" class="btn btn-outline">Starter Pack anfragen</a>
      <a href="mailto:messerjocke@proton.me?subject=Creator%20Pack%20Anfrage" class="btn btn-primary">Creator Pack anfragen</a>
      <a href="mailto:messerjocke@proton.me?subject=Custom%20Track%20Anfrage" class="btn btn-outline">Custom Track anfragen</a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <span>&copy; 2026 Messerjocke AI Music Production. Alle Rechte vorbehalten.</span>
    <span>
      <a href="services.html">Services</a>
      &nbsp;&middot;&nbsp;
      <a href="about.html">About</a>
      &nbsp;&middot;&nbsp;
      <a href="kontakt.html">Kontakt</a>
      &nbsp;&middot;&nbsp;
      <a href="impressum.html">Impressum</a>
      &nbsp;&middot;&nbsp;
      <a href="datenschutz.html">Datenschutz</a>
      &nbsp;&middot;&nbsp;
      <a href="https://open.spotify.com/artist/4DhynD1iJwYZqwdwCwu2PD" target="_blank" rel="noopener">Spotify</a>
    </span>
  </footer>

  <noscript>
    <style>
      .fade-in { opacity: 1; transform: none; }
      .nav-links { display: flex !important; }
      .faq-a { display: block; }
    </style>
  </noscript>

  <script>
    // Mobile menu toggle
    function toggleMobileMenu() {
      const btn = document.querySelector('.nav-toggle');
      const links = document.getElementById('nav-links');
      const isOpen = links.classList.toggle('open');
      btn.classList.toggle('active');
      btn.setAttribute('aria-expanded', isOpen);
    }

    // Close mobile menu on link click
    document.querySelectorAll('#nav-links a').forEach(function(link) {
      link.addEventListener('click', function() {
        document.getElementById('nav-links').classList.remove('open');
        document.querySelector('.nav-toggle').classList.remove('active');
        document.querySelector('.nav-toggle').setAttribute('aria-expanded', 'false');
      });
    });

    // FAQ Toggle
    function toggleFaq(btn) {
      const item = btn.parentElement;
      const isOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item.open').forEach(i => i.classList.remove('open'));
      if (!isOpen) item.classList.add('open');
    }

    // Scroll fade-in
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          observer.unobserve(e.target);
        }
      });
    }, { threshold: 0.1 });

    document.querySelectorAll('.fade-in').forEach(el => {
      // Elemente die beim Laden bereits im Viewport sind sofort sichtbar machen
      const rect = el.getBoundingClientRect();
      if (rect.top < window.innerHeight && rect.bottom > 0) {
        el.classList.add('visible');
      } else {
        observer.observe(el);
      }
    });

    // Nav shadow on scroll
    const nav = document.querySelector('nav');
    window.addEventListener('scroll', () => {
      nav.style.borderBottomColor = window.scrollY > 20
        ? 'rgba(124,92,255,0.2)'
        : 'rgba(30,30,46,0.6)';
    });
  </script>

</body>
</html>
